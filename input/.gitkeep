# Input Directory

此目录用于存放待拆分的Word文档文件。

## 使用说明

1. 将需要拆分的 `.docx` 或 `.doc` 文件放入此目录
2. 运行 `main.py` 脚本进行文档拆分
3. 拆分结果将保存到 `output` 目录中

## 支持的文档格式

- Microsoft Word 文档 (`.docx`)
- Microsoft Word 97-2003 文档 (`.doc`)

## 注意事项

- 确保文档包含标题结构（标题样式或大纲级别）
- 文档不能被密码保护
- 建议文档大小不超过100MB以获得最佳性能

---

*此文件 (.gitkeep) 用于在Git版本控制中保留空目录结构*